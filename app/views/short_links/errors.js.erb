
$("ul.errors").html("")
<% if @short_link.errors.any? %>
  <% @short_link.errors.messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.last.last.html_safe %>"))
  <% end %>
<% end %>
