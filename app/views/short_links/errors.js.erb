$("#error-msg").html("");
$("#success-msg").html("");

<% if @short_link.errors.any? %>
  <% @short_link.errors.messages.each do |message| %>
    $("#error-msg").append($("<li />").html("<%= message.last.last.html_safe %>"))
  <% end %>
<% end %>
