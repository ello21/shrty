$("#shrty-err-detail").html("");
$("#shrty-form-status").html("");

<% if @short_link.errors.any? %>
  <% @short_link.errors.messages.each do |message| %>
    $("#shrty-err-detail").append($("<li id='errd' />").html("<%= message.last.last.html_safe %>"))
  <% end %>
<% end %>
