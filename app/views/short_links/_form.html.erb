<%= form_for(@short_link) do |f| %>
  <% if @short_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@short_link.errors.count, "error") %> prohibited this short_link from being saved:</h2>

      <ul>
      <% @short_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_short_key %><br>
    <%= f.text_field :user_short_key %>
  </div>
  <div class="field">
    <%= f.label :destination_url %><br>
    <%= f.text_area :destination_url %>
  </div>
  <div class="field">
    <%= f.label :http_status %><br>
    <%= f.number_field :http_status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
